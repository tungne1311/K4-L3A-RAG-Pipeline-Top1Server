# Individual contribution report

## Thông tin

- Họ và tên: Đỗ Thanh Tùng
- Mã học viên: 2A202602845
- Nhóm: <Top1Server>
- Repository/branch: K4-L3A-RAG-Pipeline-Top1Server / main
- Vai trò: Data & Standardization

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit | Trạng thái |
|---|---|---|---|
| Task 1 — thu thập văn bản | Chọn nguồn, viết hàm tải có resume và kiểm tra file toàn vẹn; thu 5 văn bản của UBND Quảng Nam và UBND Hội An | `src/task1_collect_legal_docs.py`, `data/landing/legal/` — `8dd6e25` | Done |
| Task 2 — crawl bài viết | Chọn 8 URL từ danang.gov.vn, baodanang.vn, danangfantasticity.com, baovanhoa.vn; viết `crawl_article()` có nhánh dự phòng, lọc menu và rác HTML | `src/task2_crawl_news.py`, `data/landing/news/` — `8dd6e25` | Done |
| Task 3 — chuẩn hoá Markdown | Convert PDF và JSON sang Markdown, gắn header metadata để nhóm dựng citation; chuẩn hoá khoảng trắng | `src/task3_convert_markdown.py`, `data/standardized/` — `8dd6e25` | Done |
| Danh sách nguồn | Sinh bảng file ↔ tiêu đề ↔ cơ quan ban hành ↔ URL cho phần evaluation | `data/SOURCES.md` — `8dd6e25` | Done |

Corpus cuối cùng: 5 văn bản pháp quy và 8 bài viết, ra 13 file Markdown, khoảng 213.700 ký tự.

## Quyết định kỹ thuật quan trọng

**1. Tải tài liệu theo kiểu resume thay vì tải một lần.**
Lúc thử nguồn em thấy file tải về lúc 196 KB lúc 540 KB dù cùng một URL, và mở ra đều hỏng — server đóng kết nối giữa chừng. Em cho hàm tải kiểm tra file có đủ `%PDF` ở đầu và `%%EOF` ở cuối không, thiếu thì tải tiếp phần còn lại, tối đa 6 lần. File Quy chế bảo vệ di sản sau đó về đủ 1,69 MB, cả 5 văn bản đều toàn vẹn.
*Trade-off:* mỗi file có thể phải gọi mạng vài lần nên chậm hơn, đổi lại không có file hỏng lọt vào corpus.

## Kiểm thử và kết quả

`pytest tests/test_acceptance.py` pass cả 3 test thuộc phần dữ liệu. Em chạy lại Task 1→3 lần hai, số file trong `data/` vẫn là 27 nên pipeline không sinh file trùng. Em cũng viết một script nhỏ đếm lỗi font, chữ bị tách rời và khoảng trắng thừa trên cả 13 file — cả ba đều về 0.

Ba lỗi đáng kể tôi gặp:

- Log báo "Failed" nhưng file vẫn ghi thành công. Nguyên nhân là console Windows dùng cp1252 không in được tiếng Việt nên `print()` ném lỗi sau khi file đã lưu. Sửa bằng cách đặt lại encoding của stdout thành UTF-8.
- Quyết định 351/QĐ-UBND trích ra 0 ký tự vì bản PDF công bố là bản scan. Em thay bằng Quy chế quản lý di tích tỉnh Quảng Nam, và cho Task 3 bỏ qua kèm cảnh báo những file dưới 200 ký tự thay vì ghi file rỗng. Đây cũng là lý do em lấy 5 văn bản thay vì đúng 3.
- Văn bản căn đều lề khiến pdfminer chèn 2-3 dấu cách giữa các từ, một file có tới 1.328 chỗ. Em gộp lại thành một dấu cách để không lãng phí `chunk_size` của Task 4.

## Điều còn hạn chế

Em phải bỏ Quyết định 351/QĐ-UBND — một văn bản khá quan trọng về kế hoạch quản lý di sản — chỉ vì chưa có bước OCR. Ngoài ra `article_03.md` chỉ 773 ký tự, mỏng hơn hẳn các bài khác nên chắc sẽ ít đóng góp cho retrieval.

Nếu có thêm thời gian, việc đầu tiên em làm là thêm OCR (Tesseract với gói tiếng Việt) cho những PDF trích được dưới ngưỡng, để lấy lại các văn bản dạng scan thay vì loại bỏ.

## Xác nhận đóng góp

Em xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: <20/09/2026>
- Tên thành viên: Đỗ Thanh Tùng
